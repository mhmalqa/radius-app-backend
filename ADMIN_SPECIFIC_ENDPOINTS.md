# 🔗 الروابط الخاصة للمدير - عرض جميع السلايدات والبثوث

## 📋 نظرة عامة

تم إنشاء روابط خاصة للمدير لعرض **جميع** السلايدات والبثوث المباشرة (بما في ذلك غير النشطة والمخفية).

---

## 🖼️ السلايدات (Slides)

### رابط المدير - عرض جميع السلايدات

```
GET /api/admin/slides
Authorization: Bearer {token}
```

**الوصف**: عرض جميع السلايدات للمدير (نشطة + مخفية + جميع الأنواع)  
**الصلاحيات**: المدير فقط (Admin - Role: 2)

**المعاملات الاختيارية:**

-   `target_audience`: فلترة حسب الجمهور المستهدف (`all`, `active_users`, `expired_users`)
-   `is_active`: فلترة حسب الحالة (`true`/`false`)
-   `per_page`: عدد النتائج في الصفحة (افتراضي: 15)
-   `page`: رقم الصفحة (افتراضي: 1)

**مثال:**

```http
GET /api/admin/slides?is_active=false&target_audience=all&per_page=20&page=1
Authorization: Bearer 1|xxxxxxxxxxxxx
```

**الاستجابة:**

```json
{
    "success": true,
    "data": [
        {
            "id": 1,
            "title": "سلايد نشط",
            "is_active": true,
            "target_audience": "all",
            ...
        },
        {
            "id": 2,
            "title": "سلايد مخفي",
            "is_active": false,
            "target_audience": "active_users",
            ...
        }
    ],
    "meta": {
        "current_page": 1,
        "last_page": 1,
        "per_page": 15,
        "total": 2
    }
}
```

---

## 📺 البثوث المباشرة (Live Streams)

### رابط المدير - عرض جميع البثوث

```
GET /api/admin/live-streams
Authorization: Bearer {token}
```

**الوصف**: عرض جميع البثوث المباشرة للمدير (نشطة + مخفية + جميع أنواع الوصول)  
**الصلاحيات**: المدير فقط (Admin - Role: 2)

**المعاملات الاختيارية:**

-   `category`: فلترة حسب الفئة (`match`, `channel`, `event`)
-   `access_type`: فلترة حسب نوع الوصول (`all_subscribers`, `live_subscribers_only`)
-   `is_active`: فلترة حسب الحالة (`true`/`false`)
-   `featured`: فلترة حسب المميز (`true`/`false`)
-   `per_page`: عدد النتائج في الصفحة (افتراضي: 15)
-   `page`: رقم الصفحة (افتراضي: 1)

**مثال:**

```http
GET /api/admin/live-streams?is_active=false&access_type=live_subscribers_only&category=match&per_page=20&page=1
Authorization: Bearer 1|xxxxxxxxxxxxx
```

**الاستجابة:**

```json
{
    "success": true,
    "data": [
        {
            "id": 1,
            "title": "بث نشط",
            "is_active": true,
            "access_type": "all_subscribers",
            ...
        },
        {
            "id": 2,
            "title": "بث مخفي",
            "is_active": false,
            "access_type": "live_subscribers_only",
            ...
        }
    ],
    "meta": {
        "current_page": 1,
        "last_page": 1,
        "per_page": 15,
        "total": 2
    }
}
```

---

## 🔄 الفرق بين الروابط

### الروابط العامة (للمستخدمين العاديين)

| الرابط                  | الوصف                                   |
| ----------------------- | --------------------------------------- |
| `GET /api/slides`       | عرض السلايدات النشطة والمتاحة فقط       |
| `GET /api/live-streams` | عرض البثوث النشطة المتاحة حسب الصلاحيات |

### الروابط الخاصة للمدير

| الرابط                        | الوصف                                             |
| ----------------------------- | ------------------------------------------------- |
| `GET /api/admin/slides`       | عرض **جميع** السلايدات (نشطة + مخفية)             |
| `GET /api/admin/live-streams` | عرض **جميع** البثوث (نشطة + مخفية + جميع الأنواع) |

---

## ✅ المميزات

1. **عرض شامل**: المدير يرى جميع السلايدات والبثوث بدون أي فلترة
2. **فلترة متقدمة**: يمكن للمدير استخدام معاملات الفلترة لعرض محتوى محدد
3. **Pagination**: دعم التصفح عبر الصفحات
4. **الأمان**: الروابط محمية بـ Admin فقط

---

## 🔐 متطلبات المصادقة

-   **الصلاحيات**: المدير فقط (Admin - Role: 2)
-   **نوع المصادقة**: Bearer Token
-   **إرسال Token**: في Header باسم `Authorization`

---

## 📝 أمثلة الاستخدام

### عرض جميع السلايدات المخفية

```http
GET /api/admin/slides?is_active=false
Authorization: Bearer {token}
```

### عرض جميع البثوث للمشتركين فقط

```http
GET /api/admin/live-streams?access_type=live_subscribers_only
Authorization: Bearer {token}
```

### عرض جميع البثوث المخفية من فئة المباريات

```http
GET /api/admin/live-streams?is_active=false&category=match
Authorization: Bearer {token}
```

---

## ⚠️ ملاحظات

1. **الروابط العامة**: تبقى كما هي للمستخدمين العاديين
2. **الروابط الخاصة**: للمدير فقط وتتطلب صلاحيات Admin
3. **الفلترة**: جميع المعاملات اختيارية
4. **Pagination**: يتم تطبيق Pagination تلقائياً
