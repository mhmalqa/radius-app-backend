# 🎛️ دليل لوحة التحكم للمدير والمحاسب - Frontend Team

## 📋 نظرة عامة

هذا الدليل موجه لفريق تطوير الواجهة الأمامية (Frontend) لبناء لوحة تحكم شاملة تتيح للمدير والمحاسب إدارة التطبيق بشكل كامل.

---

## 🔐 مستويات الصلاحيات

### 1. 👑 المدير (Admin) - Role: 2

-   **جميع الصلاحيات** في النظام
-   يمكنه إدارة المستخدمين، طلبات الدفع، البث المباشر، السلايدات، الإشعارات، وطرق الدفع

### 2. 💰 المحاسب (Accountant) - Role: 1

-   إدارة **طلبات الدفع** فقط (عرض، قبول، رفض)
-   إدارة **المستخدمين** (عرض، تعديل، تفعيل/تعطيل، تغيير الأدوار)
-   **لا يمكنه** إدارة البث المباشر، السلايدات، أو الإشعارات

---

## 🔑 المصادقة (Authentication)

### تسجيل الدخول

```http
POST /api/auth/login
Content-Type: application/json

{
    "username": "admin001",
    "password": "password123"
}
```

**الاستجابة الناجحة:**

```json
{
    "success": true,
    "message": "تم تسجيل الدخول بنجاح",
    "data": {
        "user": {
            "id": 1,
            "username": "admin001",
            "email": "admin@example.com",
            "phone": "123456789",
            "role": 2,
            "is_active": true,
            "live_access": true,
            "language": "ar"
        },
        "token": "1|xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
    }
}
```

### الحصول على بيانات المستخدم الحالي

```http
GET /api/auth/me
Authorization: Bearer {token}
```

### تسجيل الخروج

```http
POST /api/auth/logout
Authorization: Bearer {token}
```

---

## 👥 إدارة المستخدمين (User Management)

### ⚠️ الصلاحيات

-   **المحاسب والمدير**: عرض، تعديل، تفعيل/تعطيل، تغيير الأدوار
-   **المدير فقط**: حذف المستخدمين

### 1. عرض جميع المستخدمين مع البحث والفلترة

```http
GET /api/admin/users?search=ahmed&role=0&is_active=true&live_access=true&language=ar&sort_by=created_at&sort_order=desc&per_page=15&page=1
Authorization: Bearer {token}
```

**المعاملات الاختيارية:**

-   `search` - البحث في username, phone, email
-   `role` - فلترة حسب الدور (0: user, 1: accountant, 2: admin)
-   `is_active` - فلترة حسب حالة الحساب (true/false)
-   `live_access` - فلترة حسب صلاحية البث (true/false)
-   `language` - فلترة حسب اللغة (ar/en)
-   `sort_by` - ترتيب حسب (created_at, username, etc.)
-   `sort_order` - اتجاه الترتيب (asc/desc)
-   `per_page` - عدد النتائج في الصفحة (افتراضي: 15)
-   `page` - رقم الصفحة (افتراضي: 1)

**الاستجابة:**

```json
{
    "success": true,
    "data": [
        {
            "id": 1,
            "username": "user001",
            "phone": "123456789",
            "email": "user@example.com",
            "role": 0,
            "is_active": true,
            "live_access": true,
            "language": "ar",
            "created_at": "2024-01-01T00:00:00.000000Z",
            "updated_at": "2024-01-01T00:00:00.000000Z",
            "subscription": {
                "id": 1,
                "radius_username": "user001",
                "expires_at": "2024-12-31T23:59:59.000000Z",
                "is_active": true
            }
        }
    ],
    "meta": {
        "current_page": 1,
        "last_page": 5,
        "per_page": 15,
        "total": 75,
        "from": 1,
        "to": 15
    }
}
```

**واجهة المستخدم المطلوبة:**

-   جدول المستخدمين مع:
    -   أعمدة: ID، اسم المستخدم، البريد الإلكتروني، الهاتف، الدور، الحالة (نشط/غير نشط)، صلاحية البث، اللغة
    -   شريط بحث في الأعلى
    -   فلاتر: الدور، الحالة، صلاحية البث، اللغة
    -   ترتيب قابل للتعديل
    -   ترقيم الصفحات
    -   زر "عرض التفاصيل" لكل مستخدم

### 2. عرض تفاصيل مستخدم محدد

```http
GET /api/admin/users/{id}
Authorization: Bearer {token}
```

**الاستجابة:**

```json
{
    "success": true,
    "data": {
        "id": 1,
        "username": "user001",
        "phone": "123456789",
        "email": "user@example.com",
        "role": 0,
        "is_active": true,
        "live_access": true,
        "language": "ar",
        "created_at": "2024-01-01T00:00:00.000000Z",
        "updated_at": "2024-01-01T00:00:00.000000Z",
        "subscription": {
            "id": 1,
            "radius_username": "user001",
            "expires_at": "2024-12-31T23:59:59.000000Z",
            "is_active": true
        },
        "payment_requests": [
            {
                "id": 1,
                "amount": 50000,
                "status": 1,
                "created_at": "2024-01-01T00:00:00.000000Z"
            }
        ]
    }
}
```

**واجهة المستخدم المطلوبة:**

-   صفحة تفاصيل المستخدم مع:
    -   معلومات المستخدم الأساسية
    -   معلومات الاشتراك
    -   تاريخ طلبات الدفع
    -   أزرار التعديل، التفعيل/التعطيل، تغيير الدور

### 3. تحديث مستخدم

```http
PUT /api/admin/users/{id}
Authorization: Bearer {token}
Content-Type: application/json

{
    "username": "new_username",
    "password": "new_password",
    "phone": "123456789",
    "email": "new@example.com",
    "language": "ar",
    "is_active": true,
    "live_access": true,
    "role": 0
}
```

**ملاحظات:**

-   جميع الحقول اختيارية
-   عند تغيير `username`، يتم تحديث `radius_username` في الاشتراك تلقائياً

**واجهة المستخدم المطلوبة:**

-   نموذج تعديل مع حقول:
    -   اسم المستخدم
    -   كلمة المرور (اختياري)
    -   البريد الإلكتروني
    -   الهاتف
    -   اللغة (قائمة منسدلة: ar/en)
    -   الحالة (مفتاح تبديل: نشط/غير نشط)
    -   صلاحية البث (مفتاح تبديل)
    -   الدور (قائمة منسدلة: مستخدم/محاسب/مدير)

### 4. تفعيل/تعطيل الحساب

```http
POST /api/admin/users/{id}/toggle-active
Authorization: Bearer {token}
```

**الاستجابة:**

```json
{
    "success": true,
    "message": "تم تفعيل الحساب بنجاح",
    "data": {
        "id": 1,
        "is_active": true
    }
}
```

**واجهة المستخدم المطلوبة:**

-   زر تبديل في جدول المستخدمين أو صفحة التفاصيل

### 5. تفعيل/إلغاء صلاحية البث

```http
POST /api/admin/users/{id}/toggle-live-access
Authorization: Bearer {token}
```

**واجهة المستخدم المطلوبة:**

-   زر تبديل في جدول المستخدمين أو صفحة التفاصيل

### 6. تغيير دور المستخدم

```http
POST /api/admin/users/{id}/change-role
Authorization: Bearer {token}
Content-Type: application/json

{
    "role": 1
}
```

**القيم:**

-   `0` - مستخدم عادي
-   `1` - محاسب
-   `2` - مدير

**واجهة المستخدم المطلوبة:**

-   قائمة منسدلة أو أزرار في صفحة التفاصيل

### 7. حذف مستخدم (Admin فقط)

```http
DELETE /api/admin/users/{id}
Authorization: Bearer {token}
```

**واجهة المستخدم المطلوبة:**

-   زر حذف مع تأكيد (Modal) في صفحة التفاصيل

---

## 💳 إدارة طلبات الدفع (Payment Requests Management)

### ⚠️ الصلاحيات

-   **المحاسب والمدير**: عرض جميع الطلبات، قبول/رفض الطلبات

### 1. عرض جميع طلبات الدفع

```http
GET /api/admin/payment-requests?status=0&user_id=1&payment_method_id=1&per_page=15&page=1
Authorization: Bearer {token}
```

**المعاملات الاختيارية:**

-   `status` - فلترة حسب الحالة (0: pending, 1: approved, 2: rejected)
-   `user_id` - فلترة حسب المستخدم
-   `payment_method_id` - فلترة حسب طريقة الدفع
-   `per_page` - عدد النتائج في الصفحة
-   `page` - رقم الصفحة

**الاستجابة:**

```json
{
    "success": true,
    "data": [
        {
            "id": 1,
            "user_id": 1,
            "user": {
                "id": 1,
                "username": "user001",
                "email": "user@example.com"
            },
            "payment_method_id": 1,
            "payment_method": {
                "id": 1,
                "name": "Visa",
                "name_ar": "فيزا"
            },
            "amount": 50000,
            "approved_amount": null,
            "status": 0,
            "notes": null,
            "admin_notes": null,
            "receipt_image": "receipts/receipt_123.jpg",
            "created_at": "2024-01-01T00:00:00.000000Z",
            "updated_at": "2024-01-01T00:00:00.000000Z"
        }
    ],
    "meta": {
        "current_page": 1,
        "last_page": 5,
        "per_page": 15,
        "total": 75
    }
}
```

**واجهة المستخدم المطلوبة:**

-   جدول طلبات الدفع مع:
    -   أعمدة: ID، المستخدم، طريقة الدفع، المبلغ، المبلغ الموافق عليه، الحالة، التاريخ
    -   فلاتر: الحالة، المستخدم، طريقة الدفع
    -   ترقيم الصفحات
    -   زر "عرض التفاصيل" لكل طلب
    -   زر "قبول/رفض" سريع

### 2. عرض تفاصيل طلب دفع

```http
GET /api/admin/payment-requests/{id}
Authorization: Bearer {token}
```

**واجهة المستخدم المطلوبة:**

-   صفحة تفاصيل مع:
    -   معلومات الطلب الكاملة
    -   صورة الإيصال (إن وجدت)
    -   معلومات المستخدم
    -   أزرار قبول/رفض
    -   حقل لإضافة ملاحظات

### 3. قبول/رفض طلب دفع

```http
PUT /api/admin/payment-requests/{id}/status
Authorization: Bearer {token}
Content-Type: application/json

{
    "status": 1,
    "notes": "تم قبول الطلب",
    "approved_amount": 50000
}
```

**المعاملات:**

-   `status` - الحالة الجديدة (1: approved, 2: rejected)
-   `notes` - ملاحظات (اختياري)
-   `approved_amount` - المبلغ الموافق عليه (اختياري، إذا كان مختلف عن المطلوب)

**واجهة المستخدم المطلوبة:**

-   Modal أو صفحة منفصلة مع:
    -   زر "قبول" و "رفض"
    -   حقل للملاحظات
    -   حقل للمبلغ الموافق عليه (اختياري)
    -   زر تأكيد

---

## 📺 إدارة البث المباشر (Live Streams Management)

### ⚠️ الصلاحيات

-   **المدير فقط**: إنشاء، تعديل، حذف

### 1. عرض جميع البث المباشر (للعرض العام)

```http
GET /api/live-streams?category=match&featured=true&is_active=true
```

**المعاملات الاختيارية:**

-   `category` - فلترة حسب الفئة (match, training, etc.)
-   `featured` - فلترة حسب المميز (true/false)
-   `is_active` - فلترة حسب النشط (true/false)

### 2. إنشاء بث مباشر جديد (Admin فقط)

```http
POST /api/admin/live-streams
Authorization: Bearer {token}
Content-Type: multipart/form-data

{
    "title": "مباراة اليوم",
    "description": "مباراة مهمة",
    "stream_url": "https://example.com/stream.m3u8",
    "thumbnail": [file],
    "category": "match",
    "start_time": "2024-12-25 20:00:00",
    "end_time": "2024-12-25 22:00:00",
    "is_active": true,
    "is_featured": true
}
```

**واجهة المستخدم المطلوبة:**

-   نموذج إنشاء/تعديل مع:
    -   العنوان
    -   الوصف
    -   رابط البث
    -   رفع الصورة المصغرة (Image upload)
    -   الفئة (قائمة منسدلة)
    -   وقت البداية والنهاية (Date/Time picker)
    -   مفاتيح تبديل: نشط، مميز

### 3. تحديث بث مباشر (Admin فقط)

```http
PUT /api/admin/live-streams/{id}
Authorization: Bearer {token}
Content-Type: application/json

{
    "title": "مباراة محدثة",
    "is_active": false
}
```

### 4. حذف بث مباشر (Admin فقط)

```http
DELETE /api/admin/live-streams/{id}
Authorization: Bearer {token}
```

**واجهة المستخدم المطلوبة:**

-   جدول البث المباشر مع:
    -   أعمدة: العنوان، الفئة، وقت البداية، الحالة، المميز
    -   أزرار: تعديل، حذف
    -   زر "إضافة جديد"

---

## 🖼️ إدارة السلايدات (Slides Management)

### ⚠️ الصلاحيات

-   **المدير فقط**: إنشاء، تعديل، حذف

### 1. عرض جميع السلايدات (للعرض العام)

```http
GET /api/slides?target_audience=all&is_active=true
```

### 2. إنشاء سلايد جديد (Admin فقط)

```http
POST /api/admin/slides
Authorization: Bearer {token}
Content-Type: multipart/form-data

{
    "title": "عرض خاص",
    "title_ar": "عرض خاص",
    "description": "وصف السلايد",
    "description_ar": "وصف السلايد",
    "image": [file],
    "link_url": "https://example.com",
    "target_audience": "all",
    "is_active": true,
    "sort_order": 1
}
```

**واجهة المستخدم المطلوبة:**

-   نموذج إنشاء/تعديل مع:
    -   العنوان (عربي/إنجليزي)
    -   الوصف (عربي/إنجليزي)
    -   رفع الصورة (Image upload)
    -   رابط السلايد
    -   الجمهور المستهدف (قائمة منسدلة: all, active_subscribers, etc.)
    -   ترتيب العرض
    -   مفتاح تبديل: نشط

### 3. تحديث سلايد (Admin فقط)

```http
PUT /api/admin/slides/{id}
Authorization: Bearer {token}
Content-Type: multipart/form-data

{
    "title": "عنوان محدث",
    "is_active": false
}
```

### 4. حذف سلايد (Admin فقط)

```http
DELETE /api/admin/slides/{id}
Authorization: Bearer {token}
```

**واجهة المستخدم المطلوبة:**

-   جدول السلايدات مع:
    -   معاينة الصورة
    -   العنوان، الجمهور المستهدف، الترتيب، الحالة
    -   أزرار: تعديل، حذف
    -   زر "إضافة جديد"

---

## 🔔 إدارة الإشعارات (Notifications Management)

### ⚠️ الصلاحيات

-   **المدير فقط**: إرسال إشعارات

### 1. إنشاء إشعار جديد (Admin فقط)

```http
POST /api/admin/notifications
Authorization: Bearer {token}
Content-Type: application/json

{
    "title": "عنوان الإشعار",
    "body": "محتوى الإشعار",
    "type": "manual",
    "priority": 1,
    "action_url": "/subscription",
    "action_text": "تجديد الآن",
    "target_type": "active",
    "user_ids": [1, 2, 3]
}
```

**target_type:**

-   `all` - إرسال لجميع المستخدمين النشطين
-   `active` - إرسال للمستخدمين النشطين فقط (مع اشتراك صالح)
-   `specific` - إرسال لمستخدمين محددين (يجب تحديد `user_ids`)

**واجهة المستخدم المطلوبة:**

-   نموذج إنشاء إشعار مع:
    -   العنوان
    -   المحتوى (Textarea)
    -   الأولوية (قائمة منسدلة: 0-3)
    -   رابط الإجراء (اختياري)
    -   نص زر الإجراء (اختياري)
    -   نوع المستلمين (Radio buttons: all, active, specific)
    -   اختيار المستخدمين (Multi-select، يظهر فقط عند `target_type = specific`)
    -   زر "إرسال"

### 2. عرض الإشعارات (للمستخدمين)

```http
GET /api/notifications?unread_only=true
Authorization: Bearer {token}
```

### 3. عدد الإشعارات غير المقروءة

```http
GET /api/notifications/unread-count
Authorization: Bearer {token}
```

---

## 💰 إدارة طرق الدفع (Payment Methods Management)

### ⚠️ الصلاحيات

-   **المدير فقط**: إنشاء، تعديل، حذف

### 1. عرض جميع طرق الدفع (للعرض العام)

```http
GET /api/payment-methods
```

### 2. إنشاء طريقة دفع جديدة (Admin فقط)

```http
POST /api/admin/payment-methods
Authorization: Bearer {token}
Content-Type: multipart/form-data

{
    "name": "Visa",
    "name_ar": "فيزا",
    "icon": [file],
    "qr_code": [file],
    "code": "1234567890",
    "is_active": true,
    "instructions": "أرسل المبلغ إلى الحساب أعلاه",
    "sort_order": 1
}
```

**واجهة المستخدم المطلوبة:**

-   نموذج إنشاء/تعديل مع:
    -   الاسم (إنجليزي/عربي)
    -   رفع الأيقونة (Image upload)
    -   رفع QR Code (Image upload، اختياري)
    -   الكود (نص، اختياري)
    -   التعليمات (Textarea)
    -   ترتيب العرض
    -   مفتاح تبديل: نشط

### 3. تحديث طريقة دفع (Admin فقط)

```http
PUT /api/admin/payment-methods/{id}
Authorization: Bearer {token}
Content-Type: multipart/form-data

{
    "name": "Visa Updated",
    "is_active": false
}
```

### 4. حذف طريقة دفع (Admin فقط)

```http
DELETE /api/admin/payment-methods/{id}
Authorization: Bearer {token}
```

**واجهة المستخدم المطلوبة:**

-   جدول طرق الدفع مع:
    -   معاينة الأيقونة
    -   الاسم (عربي/إنجليزي)، الحالة، الترتيب
    -   أزرار: تعديل، حذف
    -   زر "إضافة جديد"

---

## 📊 لوحة المعلومات (Dashboard)

### الإحصائيات المطلوبة

**واجهة المستخدم المطلوبة:**

-   صفحة Dashboard رئيسية تعرض:
    -   عدد المستخدمين الإجمالي
    -   عدد المستخدمين النشطين
    -   عدد طلبات الدفع المعلقة
    -   عدد طلبات الدفع المقبولة (هذا الشهر)
    -   إجمالي الإيرادات (هذا الشهر)
    -   عدد البث المباشر النشط
    -   رسوم بيانية للإحصائيات

**ملاحظة:** هذه الإحصائيات يمكن حسابها من البيانات المتاحة عبر API endpoints المذكورة أعلاه.

---

## 🎨 تصميم الواجهة (UI/UX Recommendations)

### 1. التخطيط العام

-   **Sidebar Navigation** مع:
    -   Dashboard
    -   المستخدمين
    -   طلبات الدفع
    -   البث المباشر
    -   السلايدات
    -   الإشعارات
    -   طرق الدفع (Admin فقط)
-   **Header** مع:
    -   معلومات المستخدم الحالي
    -   زر تسجيل الخروج
    -   إشعارات (Badge مع عدد غير المقروءة)
-   **Main Content Area** مع Breadcrumbs

### 2. الألوان والتصميم

-   استخدام ألوان متسقة مع هوية التطبيق
-   Dark/Light mode (اختياري)
-   تصميم Responsive للجوال والتابلت

### 3. التفاعلات

-   Loading states عند جلب البيانات
-   Toast notifications للنجاح/الفشل
-   Confirmation modals للعمليات الحساسة (حذف، تغيير الدور)
-   Pagination للجداول الكبيرة
-   Search و Filters في كل صفحة

### 4. إدارة الحالة (State Management)

-   حفظ Token في localStorage أو secure storage
-   إعادة توجيه تلقائي عند انتهاء صلاحية Token
-   تحديث البيانات تلقائياً عند الحاجة

---

## 🚨 معالجة الأخطاء

### حالات الخطأ الشائعة

#### 401 Unauthorized

```json
{
    "success": false,
    "message": "غير مصرح"
}
```

**الإجراء:** إعادة توجيه لصفحة تسجيل الدخول

#### 403 Forbidden

```json
{
    "success": false,
    "message": "ليس لديك صلاحية للوصول إلى هذا المورد"
}
```

**الإجراء:** إخفاء العناصر غير المسموحة أو إظهار رسالة خطأ

#### 422 Validation Error

```json
{
    "success": false,
    "message": "خطأ في التحقق من البيانات",
    "errors": {
        "email": ["البريد الإلكتروني غير صحيح"],
        "phone": ["رقم الهاتف مطلوب"]
    }
}
```

**الإجراء:** عرض رسائل الخطأ بجانب الحقول

#### 404 Not Found

```json
{
    "success": false,
    "message": "المورد غير موجود"
}
```

**الإجراء:** إظهار رسالة خطأ أو إعادة توجيه

#### 500 Server Error

```json
{
    "success": false,
    "message": "خطأ في الخادم"
}
```

**الإجراء:** إظهار رسالة خطأ عامة

---

## 📱 أمثلة على الاستخدام

### مثال 1: جلب قائمة المستخدمين مع البحث

```javascript
// React/Vue/Angular example
const fetchUsers = async (filters) => {
    const params = new URLSearchParams({
        search: filters.search || "",
        role: filters.role || "",
        is_active: filters.is_active ?? "",
        per_page: 15,
        page: filters.page || 1,
    });

    const response = await fetch(`/api/admin/users?${params}`, {
        headers: {
            Authorization: `Bearer ${token}`,
            "Content-Type": "application/json",
        },
    });

    const data = await response.json();
    return data;
};
```

### مثال 2: قبول طلب دفع

```javascript
const approvePaymentRequest = async (requestId, notes, approvedAmount) => {
    const response = await fetch(
        `/api/admin/payment-requests/${requestId}/status`,
        {
            method: "PUT",
            headers: {
                Authorization: `Bearer ${token}`,
                "Content-Type": "application/json",
            },
            body: JSON.stringify({
                status: 1, // 1 = approved
                notes: notes,
                approved_amount: approvedAmount,
            }),
        }
    );

    const data = await response.json();
    return data;
};
```

### مثال 3: إرسال إشعار

```javascript
const sendNotification = async (notificationData) => {
    const response = await fetch("/api/admin/notifications", {
        method: "POST",
        headers: {
            Authorization: `Bearer ${token}`,
            "Content-Type": "application/json",
        },
        body: JSON.stringify({
            title: notificationData.title,
            body: notificationData.body,
            target_type: notificationData.targetType, // 'all', 'active', 'specific'
            user_ids: notificationData.userIds || [],
            priority: notificationData.priority || 0,
            action_url: notificationData.actionUrl || "",
            action_text: notificationData.actionText || "",
        }),
    });

    const data = await response.json();
    return data;
};
```

---

## ✅ Checklist للتطوير

### الصفحات المطلوبة

-   [ ] صفحة تسجيل الدخول
-   [ ] Dashboard الرئيسية
-   [ ] صفحة إدارة المستخدمين (قائمة + تفاصيل)
-   [ ] صفحة إدارة طلبات الدفع (قائمة + تفاصيل)
-   [ ] صفحة إدارة البث المباشر (Admin فقط)
-   [ ] صفحة إدارة السلايدات (Admin فقط)
-   [ ] صفحة إرسال الإشعارات (Admin فقط)
-   [ ] صفحة إدارة طرق الدفع (Admin فقط)

### الميزات المطلوبة

-   [ ] نظام المصادقة (Login/Logout)
-   [ ] إدارة Token (حفظ، تحديث، حذف)
-   [ ] التحقق من الصلاحيات (Role-based access)
-   [ ] البحث والفلترة في الجداول
-   [ ] الترقيم (Pagination)
-   [ ] رفع الملفات (Images)
-   [ ] Toast Notifications
-   [ ] Loading States
-   [ ] Error Handling
-   [ ] Responsive Design

---

## 📞 الدعم

لأي استفسارات أو مشاكل تقنية، يرجى التواصل مع فريق Backend.

---

**تم إنشاء هذا الدليل بتاريخ:** 2025-12-25  
**آخر تحديث:** 2025-12-25
