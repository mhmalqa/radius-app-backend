# دليل الفلترة والبحث لطلبات الدفع

## 📍 الرابط الأساسي

```
GET /api/payment-requests/all
```

**المتطلبات:**
- يجب أن يكون المستخدم مسؤول (Admin) أو محاسب (Accountant)
- مطلوب توكن المصادقة في Header: `Authorization: Bearer {token}`

---

## 🔍 معاملات البحث والفلترة

### 1. البحث النصي (`search`)
البحث في: اسم المستخدم، الاسم الأول، رقم الهاتف، البريد الإلكتروني

```
?search=أحمد
```

### 2. الفلترة حسب الحالة (`status`)
- `0` = قيد المراجعة
- `1` = مقبول
- `2` = مرفوض

```
?status=1
```

### 3. الفلترة حسب حالة الدفع (`is_paid`)
- `true` = مدفوع
- `false` = غير مدفوع

```
?is_paid=true
```

### 4. الفلترة حسب الدفع المؤجل (`is_deferred`)
- `true` = دفع مؤجل
- `false` = دفع عادي

```
?is_deferred=false
```

### 5. الفلترة حسب العملة (`currency`)
- `USD` = دولار
- `SYP` = ليرة سورية
- `TRY` = ليرة تركية

```
?currency=USD
```

### 6. الفلترة حسب عدد الأشهر (`period_months`)
عدد أشهر الاشتراك المدفوعة

```
?period_months=3
```

---

## 📊 مثال على الاستخدام

### البحث عن مستخدم معين:
```
GET /api/payment-requests/all?search=أحمد
```

### فلترة متعددة:
```
GET /api/payment-requests/all?status=1&is_paid=true&currency=USD&period_months=3&search=محمد
```

### مع التصفح (Pagination):
```
GET /api/payment-requests/all?status=1&page=2&per_page=20
```

---

## 📦 شكل الاستجابة

```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "user": {
        "id": 5,
        "username": "user001",
        "firstname": "أحمد",
        "phone": "07501234567",
        "email": "user@example.com"
      },
      "period_months": 3,
      "amount": 50.00,
      "currency": "USD",
      "status": 1,
      "status_label": "مقبول",
      "is_paid": true,
      "payment_type": "online",
      "payment_date": "2025-01-15",
      "created_at": "2025-01-15T10:30:00Z"
    }
  ],
  "meta": {
    "current_page": 1,
    "last_page": 5,
    "per_page": 15,
    "total": 75
  }
}
```

---

## ✅ الحقول المهمة للعرض

### معلومات المستخدم:
- `user.username` - اسم المستخدم
- `user.firstname` - الاسم الأول
- `user.phone` - رقم الهاتف
- `user.email` - البريد الإلكتروني

### معلومات الدفع:
- `period_months` - عدد أشهر الاشتراك
- `amount` - المبلغ
- `currency` - العملة
- `status_label` - حالة الطلب (نصي)
- `is_paid` - هل تم الدفع
- `payment_type_label` - نوع الدفع (نصي)

---

## ⚡ نصائح للأداء

1. **استخدم الفلترة**: لا تجلب كل البيانات ثم تفلترها في الفرونت إند
2. **استخدم Pagination**: استخدم `page` و `per_page` للتحكم في عدد النتائج
3. **البحث**: استخدم `search` للبحث السريع في جميع حقول المستخدم
4. **الدمج**: يمكن دمج جميع المعاملات معاً للحصول على نتائج دقيقة

---

## 🔒 الأمان

- جميع الطلبات تتطلب مصادقة
- فقط المسؤولون والمحاسبون يمكنهم الوصول
- جميع المعاملات يتم التحقق منها قبل المعالجة

---

## 📝 ملاحظات

- البحث النصي (`search`) يبحث في: username, firstname, phone, email
- يمكن استخدام جميع المعاملات معاً
- النتائج مرتبة حسب تاريخ الإنشاء (الأحدث أولاً)
- عدد النتائج الافتراضي: 15 عنصر لكل صفحة

