# 📋 مثال على استجابة Radius API

## الاستجابة الفعلية

```json
{
    "status": "success",
    "username": "001",
    "firstname": "الشيخ احمد الطحبش",
    "mobile": "",
    "service": "2M-PPP",
    "price": "8.000000",
    "expiration": "2026-01-02 10:00:00",
    "active": true,
    "online": false,
    "speed": {
        "download_kbps": 2097152,
        "upload_kbps": 20971520,
        "download_mbps": 2097.2,
        "upload_mbps": 20971.5
    },
    "usage": {
        "download_MB": 8371.59,
        "upload_MB": 67316.8,
        "total_MB": 75688.39
    }
}
```

## التحويل إلى قاعدة البيانات

### الحقول المحفوظة في `user_subscriptions`:

| حقل قاعدة البيانات | القيمة من Radius | القيمة المحفوظة |
|-------------------|------------------|----------------|
| `expiration_at` | `"2026-01-02 10:00:00"` | `"2026-01-02 10:00:00"` |
| `balance` | `"8.000000"` | `8.00` |
| `plan_name` | `"2M-PPP"` | `"2M-PPP"` |
| `data_used` | `usage.total_MB: 75688.39` | `79352827904` (bytes) |
| `data_limit` | - | `null` (غير متوفر في الاستجابة) |

### حساب التحويل:

```
data_used = 75688.39 MB × 1024 × 1024
         = 75688.39 × 1048576
         = 79352827904 bytes
```

## الحقول الإضافية (غير محفوظة حالياً)

يمكن إضافتها لاحقاً إذا لزم الأمر:

- `active`: حالة الاشتراك (نشط/غير نشط)
- `online`: حالة الاتصال (متصل/غير متصل)
- `speed`: معلومات السرعة (download/upload)
- `usage`: تفاصيل الاستخدام (download/upload/total)

## مثال على الاستخدام

```php
// بعد المزامنة
$subscription = $user->subscription;

// البيانات المحفوظة
$subscription->expiration_at;  // "2026-01-02 10:00:00"
$subscription->balance;        // 8.00
$subscription->plan_name;       // "2M-PPP"
$subscription->data_used;      // 79352827904 (bytes)

// Methods مساعدة
$subscription->getDataUsagePercentage();  // نسبة الاستخدام
$subscription->getRemainingData();       // البيانات المتبقية
$subscription->isExpired();              // هل منتهي؟
$subscription->isActive();               // هل نشط؟
```

