# 📋 دليل شامل لجميع API Endpoints لتصميم لوحة التحكم

## 🔐 مستويات الصلاحيات

-   **👤 User (0)**: مستخدم عادي
-   **💰 Accountant (1)**: محاسب
-   **👑 Admin (2)**: مدير

---

## 🌐 Routes عامة (Public - لا تحتاج مصادقة)

### 1. تسجيل الدخول

```
POST /api/auth/login
```

**الوصف**: تسجيل دخول المستخدم والحصول على Token  
**المعاملات**: `username`, `password`  
**الاستجابة**: `user` object + `token`

---

### 2. التسجيل

```
POST /api/auth/register
```

**الوصف**: إنشاء حساب جديد (مع التحقق من Radius)  
**المعاملات**: `username`, `password`, `password_confirmation`, `phone`, `email`, `language`  
**الاستجابة**: `user` object + `token`

---

### 3. عرض طرق الدفع

```
GET /api/payment-methods
```

**الوصف**: عرض جميع طرق الدفع المتاحة والنشطة  
**الاستجابة**: قائمة بطرق الدفع مع الأيقونات والتعليمات

---

### 4. عرض السلايدات

```
GET /api/slides?target_audience=all&is_active=true
```

**الوصف**: عرض جميع السلايدات النشطة  
**المعاملات الاختيارية**: `target_audience` (all, active_users, expired_users)  
**الاستجابة**: قائمة بالسلايدات مع الصور والروابط

---

### 5. عرض البث المباشر

```
GET /api/live-streams?category=match&featured=true&is_active=true
```

**الوصف**: عرض جميع البث المباشر النشط  
**المعاملات الاختيارية**: `category`, `featured`, `is_active`  
**الاستجابة**: قائمة بالبث المباشر

---

### 6. عرض إعدادات التطبيق

```
GET /api/app-settings
```

**الوصف**: عرض جميع إعدادات التطبيق العامة  
**الاستجابة**: قائمة بالإعدادات

---

### 7. عرض إعداد محدد

```
GET /api/app-settings/key/{key}
```

**الوصف**: عرض إعداد محدد حسب المفتاح  
**الاستجابة**: قيمة الإعداد

---

## 🔒 Routes محمية (تحتاج مصادقة - auth:sanctum)

### 📱 Auth Routes

#### 8. الحصول على المستخدم الحالي

```
GET /api/auth/me
Authorization: Bearer {token}
```

**الوصف**: الحصول على بيانات المستخدم المسجل حالياً  
**الصلاحيات**: جميع المستخدمين  
**الاستجابة**: بيانات المستخدم الكاملة مع الدور

---

#### 9. تسجيل الخروج

```
POST /api/auth/logout
Authorization: Bearer {token}
```

**الوصف**: تسجيل خروج المستخدم (حذف الـ token)  
**الصلاحيات**: جميع المستخدمين

---

### 👤 User Routes

#### 10. عرض الملف الشخصي

```
GET /api/user/profile
Authorization: Bearer {token}
```

**الوصف**: عرض الملف الشخصي مع بيانات الاشتراك المحفوظة  
**الصلاحيات**: جميع المستخدمين  
**الاستجابة**: بيانات المستخدم + معلومات الاشتراك

---

#### 11. تحديث الملف الشخصي

```
PUT /api/user/profile
Authorization: Bearer {token}
```

**الوصف**: تحديث بيانات الملف الشخصي  
**الصلاحيات**: جميع المستخدمين  
**المعاملات**: `phone`, `email`, `language`, `device_token`, `device_type`

---

#### 12. مزامنة الاشتراك من Radius

```
POST /api/user/sync-subscription
Authorization: Bearer {token}
```

**الوصف**: مزامنة بيانات الاشتراك من Radius وحفظها في قاعدة البيانات  
**الصلاحيات**: جميع المستخدمين

---

#### 13. قراءة الاشتراك مباشرة من Radius

```
GET /api/user/subscription-from-radius
Authorization: Bearer {token}
```

**الوصف**: جلب بيانات الاشتراك مباشرة من Radius API بدون حفظ  
**الصلاحيات**: جميع المستخدمين  
**الاستجابة**: بيانات الاشتراك الحية (مثل `is_online`)

---

#### 14. عرض إيرادات المستخدم

```
GET /api/user/revenues
Authorization: Bearer {token}
```

**الوصف**: عرض إيرادات المستخدم (طلبات الدفع المقبولة)  
**الصلاحيات**: جميع المستخدمين

---

#### 15. عرض طلبات الدفع الخاصة بي

```
GET /api/user/payments
Authorization: Bearer {token}
```

**الوصف**: عرض جميع طلبات الدفع الخاصة بالمستخدم  
**الصلاحيات**: جميع المستخدمين

---

### 💳 Payment Requests Routes

#### 16. عرض طلبات الدفع

```
GET /api/payment-requests?status=0
Authorization: Bearer {token}
```

**الوصف**:

-   **المستخدم العادي**: عرض طلباته الخاصة فقط
-   **المحاسب/المدير**: عرض جميع الطلبات  
    **المعاملات الاختيارية**: `status` (0: pending, 1: approved, 2: rejected)  
    **الاستجابة**: قائمة بطلبات الدفع مع Pagination

---

#### 17. إنشاء طلب دفع جديد

```
POST /api/payment-requests
Authorization: Bearer {token}
Content-Type: multipart/form-data
```

**الوصف**: إنشاء طلب دفع جديد مع رفع إشعار الدفع  
**الصلاحيات**: جميع المستخدمين  
**المعاملات**: `amount`, `currency`, `period_months`, `payment_method_id`, `transaction_number`, `receipt_file`, `payment_date`

---

#### 18. عرض طلب دفع محدد

```
GET /api/payment-requests/{id}
Authorization: Bearer {token}
```

**الوصف**: عرض تفاصيل طلب دفع محدد  
**الصلاحيات**:

-   المستخدم: فقط طلباته الخاصة
-   المحاسب/المدير: جميع الطلبات

---

### 📺 Live Streams Routes

#### 19. عرض بث مباشر محدد

```
GET /api/live-streams/{id}
Authorization: Bearer {token}
```

**الوصف**: عرض تفاصيل بث مباشر محدد  
**الصلاحيات**: جميع المستخدمين (يجب أن يكون لديه `live_access = true`)  
**ملاحظة**: يتم زيادة `view_count` تلقائياً

---

### 🖼️ Slides Routes

#### 20. تتبع النقرات على السلايد

```
POST /api/slides/{id}/track-click
Authorization: Bearer {token}
```

**الوصف**: تسجيل نقرة على سلايد (لإحصائيات)  
**الصلاحيات**: جميع المستخدمين

---

### 🔔 Notifications Routes

#### 21. عرض الإشعارات

```
GET /api/notifications?unread_only=true
Authorization: Bearer {token}
```

**الوصف**: عرض جميع الإشعارات الخاصة بالمستخدم  
**الصلاحيات**: جميع المستخدمين  
**المعاملات الاختيارية**: `unread_only` (true/false)

---

#### 22. عدد الإشعارات غير المقروءة

```
GET /api/notifications/unread-count
Authorization: Bearer {token}
```

**الوصف**: الحصول على عدد الإشعارات غير المقروءة  
**الصلاحيات**: جميع المستخدمين  
**الاستجابة**: `{ "count": 5 }`

---

#### 23. تحديد إشعار كمقروء

```
POST /api/notifications/{id}/mark-as-read
Authorization: Bearer {token}
```

**الوصف**: تحديد إشعار محدد كمقروء  
**الصلاحيات**: جميع المستخدمين

---

#### 24. تحديد جميع الإشعارات كمقروءة

```
POST /api/notifications/mark-all-as-read
Authorization: Bearer {token}
```

**الوصف**: تحديد جميع الإشعارات كمقروءة  
**الصلاحيات**: جميع المستخدمين

---

### 🔧 Maintenance Requests Routes

#### 25. عرض طلبات الصيانة

```
GET /api/maintenance-requests
Authorization: Bearer {token}
```

**الوصف**: عرض طلبات الصيانة الخاصة بالمستخدم  
**الصلاحيات**: جميع المستخدمين

---

#### 26. إنشاء طلب صيانة

```
POST /api/maintenance-requests
Authorization: Bearer {token}
```

**الوصف**: إنشاء طلب صيانة جديد  
**الصلاحيات**: جميع المستخدمين  
**المعاملات**: `address`, `description`

---

#### 27. عرض طلب صيانة محدد

```
GET /api/maintenance-requests/{id}
Authorization: Bearer {token}
```

**الوصف**: عرض تفاصيل طلب صيانة محدد  
**الصلاحيات**: المستخدم (طلباته فقط) أو المحاسب/المدير (جميع الطلبات)

---

## 👨‍💼 Routes للمحاسب والمدير (role:admin,accountant)

### 💰 Payment Requests Management

#### 28. عرض جميع طلبات الدفع

```
GET /api/admin/payment-requests?status=0&user_id=1&payment_method_id=1&per_page=15&page=1
Authorization: Bearer {token}
```

**الوصف**: عرض جميع طلبات الدفع في النظام  
**الصلاحيات**: المحاسب والمدير فقط  
**المعاملات الاختيارية**: `status`, `user_id`, `payment_method_id`, `per_page`, `page`  
**الاستخدام في Dashboard**: صفحة إدارة طلبات الدفع

---

#### 29. قبول/رفض طلب دفع

```
PUT /api/admin/payment-requests/{id}/status
Authorization: Bearer {token}
```

**الوصف**: قبول أو رفض طلب دفع  
**الصلاحيات**: المحاسب والمدير فقط  
**المعاملات**: `status` (1: approved, 2: rejected), `notes`, `approved_amount`, `reject_reason`  
**الاستخدام في Dashboard**: Modal أو صفحة تفاصيل طلب الدفع

---

#### 30. إنشاء دفع نقدي مباشر

```
POST /api/admin/payment-requests/cash-payment
Authorization: Bearer {token}
```

**الوصف**: إنشاء طلب دفع نقدي مباشر (بدون انتظار موافقة)  
**الصلاحيات**: المحاسب والمدير فقط  
**المعاملات**: `user_id`, `amount`, `payment_method_id`, `payment_date`, `notes`

---

#### 31. تحديد طلب دفع كمدفوع

```
POST /api/admin/payment-requests/{id}/mark-as-paid
Authorization: Bearer {token}
```

**الوصف**: تحديد طلب دفع كمدفوع (لطلبات الدفع المؤجلة)  
**الصلاحيات**: المحاسب والمدير فقط

---

### 👥 User Management

#### 32. عرض جميع المستخدمين

```
GET /api/admin/users?search=ahmed&role=0&is_active=true&live_access=true&language=ar&sort_by=created_at&sort_order=desc&per_page=15&page=1
Authorization: Bearer {token}
```

**الوصف**: عرض جميع المستخدمين مع البحث والفلترة  
**الصلاحيات**: المحاسب والمدير فقط  
**المعاملات الاختيارية**: `search`, `role`, `is_active`, `live_access`, `language`, `sort_by`, `sort_order`, `per_page`, `page`  
**الاستخدام في Dashboard**: صفحة إدارة المستخدمين الرئيسية

---

#### 33. عرض تفاصيل مستخدم محدد

```
GET /api/admin/users/{id}
Authorization: Bearer {token}
```

**الوصف**: عرض تفاصيل مستخدم محدد مع طلبات الدفع والاشتراك  
**الصلاحيات**: المحاسب والمدير فقط  
**الاستخدام في Dashboard**: صفحة تفاصيل المستخدم

---

#### 34. تحديث مستخدم

```
PUT /api/admin/users/{id}
Authorization: Bearer {token}
```

**الوصف**: تحديث بيانات مستخدم  
**الصلاحيات**: المحاسب والمدير فقط  
**المعاملات**: `username`, `password`, `phone`, `email`, `language`, `is_active`, `live_access`, `role` (جميعها اختيارية)

---

#### 35. تفعيل/تعطيل الحساب

```
POST /api/admin/users/{id}/toggle-active
Authorization: Bearer {token}
```

**الوصف**: تفعيل أو تعطيل حساب مستخدم  
**الصلاحيات**: المحاسب والمدير فقط  
**الاستخدام في Dashboard**: زر تبديل في جدول المستخدمين

---

#### 36. تفعيل/إلغاء صلاحية البث

```
POST /api/admin/users/{id}/toggle-live-access
Authorization: Bearer {token}
```

**الوصف**: تفعيل أو إلغاء صلاحية مشاهدة البث المباشر  
**الصلاحيات**: المحاسب والمدير فقط  
**الاستخدام في Dashboard**: زر تبديل في جدول المستخدمين

---

#### 37. تغيير دور المستخدم

```
POST /api/admin/users/{id}/change-role
Authorization: Bearer {token}
```

**الوصف**: تغيير دور المستخدم (0: user, 1: accountant, 2: admin)  
**الصلاحيات**: المحاسب والمدير فقط  
**المعاملات**: `role` (0, 1, أو 2)

---

### 🔧 Maintenance Requests Management

#### 38. عرض جميع طلبات الصيانة

```
GET /api/admin/maintenance-requests?status=pending&per_page=15&page=1
Authorization: Bearer {token}
```

**الوصف**: عرض جميع طلبات الصيانة في النظام  
**الصلاحيات**: المحاسب والمدير فقط  
**المعاملات الاختيارية**: `status`, `per_page`, `page`  
**الاستخدام في Dashboard**: صفحة إدارة طلبات الصيانة

---

#### 39. تحديث حالة طلب الصيانة

```
PUT /api/admin/maintenance-requests/{id}/status
Authorization: Bearer {token}
```

**الوصف**: تحديث حالة طلب الصيانة (pending, in_progress, completed, cancelled)  
**الصلاحيات**: المحاسب والمدير فقط  
**المعاملات**: `status`, `assigned_to_id`, `notes`

---

### 💰 Revenues Management

#### 40. عرض جميع الإيرادات

```
GET /api/admin/revenues?start_date=2024-01-01&end_date=2024-12-31&per_page=15&page=1
Authorization: Bearer {token}
```

**الوصف**: عرض جميع الإيرادات (طلبات الدفع المقبولة)  
**الصلاحيات**: المحاسب والمدير فقط  
**المعاملات الاختيارية**: `start_date`, `end_date`, `user_id`, `payment_method_id`, `per_page`, `page`  
**الاستخدام في Dashboard**: صفحة الإيرادات

---

#### 41. ملخص الإيرادات

```
GET /api/admin/revenues/summary?start_date=2024-01-01&end_date=2024-12-31
Authorization: Bearer {token}
```

**الوصف**: الحصول على ملخص الإيرادات (إجمالي، حسب الشهر، حسب طريقة الدفع)  
**الصلاحيات**: المحاسب والمدير فقط  
**المعاملات الاختيارية**: `start_date`, `end_date`  
**الاستخدام في Dashboard**: Dashboard الرئيسية (إحصائيات)

---

#### 42. المدفوعات المؤجلة

```
GET /api/admin/revenues/deferred-payments
Authorization: Bearer {token}
```

**الوصف**: عرض المدفوعات المؤجلة (طلبات دفع مقبولة ولكن لم يتم دفعها بعد)  
**الصلاحيات**: المحاسب والمدير فقط  
**الاستخدام في Dashboard**: صفحة المدفوعات المؤجلة

---

## 👑 Routes للمدير فقط (role:admin)

### 📺 Live Streams Management

#### 43. إنشاء بث مباشر جديد

```
POST /api/admin/live-streams
Authorization: Bearer {token}
```

**الوصف**: إنشاء بث مباشر جديد  
**الصلاحيات**: المدير فقط  
**المعاملات**: `title`, `title_ar`, `description`, `description_ar`, `stream_url`, `thumbnail_url`, `category`, `start_time`, `end_time`, `is_active`, `is_featured`  
**الاستخدام في Dashboard**: صفحة إدارة البث المباشر

---

#### 44. تحديث بث مباشر

```
PUT /api/admin/live-streams/{id}
Authorization: Bearer {token}
```

**الوصف**: تحديث بيانات بث مباشر موجود  
**الصلاحيات**: المدير فقط  
**المعاملات**: نفس معاملات الإنشاء (جميعها اختيارية)

---

#### 45. حذف بث مباشر

```
DELETE /api/admin/live-streams/{id}
Authorization: Bearer {token}
```

**الوصف**: حذف بث مباشر  
**الصلاحيات**: المدير فقط

---

### 🖼️ Slides Management

#### 46. إنشاء سلايد جديد

```
POST /api/admin/slides
Authorization: Bearer {token}
Content-Type: multipart/form-data
```

**الوصف**: إنشاء سلايد إعلاني جديد  
**الصلاحيات**: المدير فقط  
**المعاملات**: `title`, `title_ar`, `description`, `description_ar`, `image` (file), `link_url`, `target_audience`, `is_active`, `sort_order`, `start_at`, `end_at`  
**الاستخدام في Dashboard**: صفحة إدارة السلايدات

---

#### 47. تحديث سلايد

```
PUT /api/admin/slides/{id}
Authorization: Bearer {token}
Content-Type: multipart/form-data
```

**الوصف**: تحديث بيانات سلايد موجود  
**الصلاحيات**: المدير فقط  
**المعاملات**: نفس معاملات الإنشاء (جميعها اختيارية)

---

#### 48. حذف سلايد

```
DELETE /api/admin/slides/{id}
Authorization: Bearer {token}
```

**الوصف**: حذف سلايد  
**الصلاحيات**: المدير فقط

---

### 🔔 Notifications Management

#### 49. إنشاء وإرسال إشعار

```
POST /api/admin/notifications
Authorization: Bearer {token}
```

**الوصف**: إنشاء وإرسال إشعار لجميع المستخدمين أو مستخدمين محددين  
**الصلاحيات**: المدير فقط  
**المعاملات**: `title`, `body`, `type`, `priority`, `action_url`, `action_text`, `target_type` (all/active/specific), `user_ids` (إذا كان target_type = specific)  
**الاستخدام في Dashboard**: صفحة إرسال الإشعارات

---

### 👥 User Management (Delete)

#### 50. حذف مستخدم

```
DELETE /api/admin/users/{id}
Authorization: Bearer {token}
```

**الوصف**: حذف مستخدم من النظام  
**الصلاحيات**: المدير فقط  
**الاستخدام في Dashboard**: زر حذف في صفحة تفاصيل المستخدم (مع تأكيد)

---

### 💳 Payment Methods Management

#### 51. إنشاء طريقة دفع جديدة

```
POST /api/admin/payment-methods
Authorization: Bearer {token}
Content-Type: multipart/form-data
```

**الوصف**: إنشاء طريقة دفع جديدة  
**الصلاحيات**: المدير فقط  
**المعاملات**: `name`, `name_ar`, `icon` (file), `qr_code` (file, optional), `code`, `is_active`, `instructions`, `sort_order`  
**الاستخدام في Dashboard**: صفحة إدارة طرق الدفع

---

#### 52. تحديث طريقة دفع

```
PUT /api/admin/payment-methods/{id}
Authorization: Bearer {token}
Content-Type: multipart/form-data
```

**الوصف**: تحديث بيانات طريقة دفع موجودة  
**الصلاحيات**: المدير فقط  
**المعاملات**: نفس معاملات الإنشاء (جميعها اختيارية)

---

#### 53. حذف طريقة دفع

```
DELETE /api/admin/payment-methods/{id}
Authorization: Bearer {token}
```

**الوصف**: حذف طريقة دفع  
**الصلاحيات**: المدير فقط

---

### ⚙️ App Settings Management

#### 54. عرض جميع الإعدادات

```
GET /api/admin/app-settings
Authorization: Bearer {token}
```

**الوصف**: عرض جميع إعدادات التطبيق (للمدير)  
**الصلاحيات**: المدير فقط  
**الاستخدام في Dashboard**: صفحة إعدادات التطبيق

---

#### 55. عرض إعداد محدد

```
GET /api/admin/app-settings/{id}
Authorization: Bearer {token}
```

**الوصف**: عرض إعداد محدد  
**الصلاحيات**: المدير فقط

---

#### 56. تحديث إعداد

```
PUT /api/admin/app-settings/{id}
Authorization: Bearer {token}
```

**الوصف**: تحديث إعداد محدد  
**الصلاحيات**: المدير فقط  
**المعاملات**: `key`, `value`, `value_ar`, `description`, `description_ar`, `type`, `is_active`

---

#### 57. تحديث عدة إعدادات دفعة واحدة

```
POST /api/admin/app-settings/update-multiple
Authorization: Bearer {token}
```

**الوصف**: تحديث عدة إعدادات دفعة واحدة  
**الصلاحيات**: المدير فقط  
**المعاملات**: `settings` (array of {id, value, value_ar})

---

#### 58. حذف إعداد

```
DELETE /api/admin/app-settings/{id}
Authorization: Bearer {token}
```

**الوصف**: حذف إعداد  
**الصلاحيات**: المدير فقط

---

### 🗄️ Database Operations

#### 59. أحدث عمليات قاعدة البيانات

```
GET /api/admin/database/operations/latest?limit=50
Authorization: Bearer {token}
```

**الوصف**: عرض أحدث عمليات قاعدة البيانات (للمراقبة)  
**الصلاحيات**: المدير فقط  
**المعاملات الاختيارية**: `limit` (افتراضي: 50)  
**الاستخدام في Dashboard**: صفحة مراقبة قاعدة البيانات

---

#### 60. جميع عمليات قاعدة البيانات

```
GET /api/admin/database/operations/all?per_page=50&page=1
Authorization: Bearer {token}
```

**الوصف**: عرض جميع عمليات قاعدة البيانات مع Pagination  
**الصلاحيات**: المدير فقط

---

#### 61. فحص أمان قاعدة البيانات

```
GET /api/admin/database/security-check
Authorization: Bearer {token}
```

**الوصف**: فحص أمان قاعدة البيانات (الكشف عن مشاكل محتملة)  
**الصلاحيات**: المدير فقط  
**الاستخدام في Dashboard**: صفحة الأمان

---

## 📊 ملخص الصفحات المطلوبة للوحة التحكم

### صفحات مشتركة (للمحاسب والمدير)

1. **صفحة تسجيل الدخول**

    - `POST /api/auth/login`

2. **Dashboard الرئيسية**

    - `GET /api/admin/users` (للإحصائيات)
    - `GET /api/admin/payment-requests?status=0` (للإحصائيات)
    - `GET /api/admin/revenues/summary` (للإحصائيات)

3. **صفحة إدارة المستخدمين**

    - `GET /api/admin/users` (قائمة)
    - `GET /api/admin/users/{id}` (تفاصيل)
    - `PUT /api/admin/users/{id}` (تعديل)
    - `POST /api/admin/users/{id}/toggle-active` (تفعيل/تعطيل)
    - `POST /api/admin/users/{id}/toggle-live-access` (صلاحية البث)
    - `POST /api/admin/users/{id}/change-role` (تغيير الدور)
    - `DELETE /api/admin/users/{id}` (حذف - Admin فقط)

4. **صفحة إدارة طلبات الدفع**

    - `GET /api/admin/payment-requests` (قائمة)
    - `GET /api/admin/payment-requests/{id}` (تفاصيل)
    - `PUT /api/admin/payment-requests/{id}/status` (قبول/رفض)
    - `POST /api/admin/payment-requests/cash-payment` (دفع نقدي)
    - `POST /api/admin/payment-requests/{id}/mark-as-paid` (تحديد كمدفوع)

5. **صفحة الإيرادات**

    - `GET /api/admin/revenues` (قائمة)
    - `GET /api/admin/revenues/summary` (ملخص)
    - `GET /api/admin/revenues/deferred-payments` (المؤجلة)

6. **صفحة إدارة طلبات الصيانة**
    - `GET /api/admin/maintenance-requests` (قائمة)
    - `GET /api/admin/maintenance-requests/{id}` (تفاصيل)
    - `PUT /api/admin/maintenance-requests/{id}/status` (تحديث الحالة)

### صفحات للمدير فقط

7. **صفحة إدارة البث المباشر**

    - `GET /api/live-streams` (قائمة - public)
    - `POST /api/admin/live-streams` (إنشاء)
    - `PUT /api/admin/live-streams/{id}` (تعديل)
    - `DELETE /api/admin/live-streams/{id}` (حذف)

8. **صفحة إدارة السلايدات**

    - `GET /api/slides` (قائمة - public)
    - `POST /api/admin/slides` (إنشاء)
    - `PUT /api/admin/slides/{id}` (تعديل)
    - `DELETE /api/admin/slides/{id}` (حذف)

9. **صفحة إرسال الإشعارات**

    - `POST /api/admin/notifications` (إرسال)

10. **صفحة إدارة طرق الدفع**

    - `GET /api/payment-methods` (قائمة - public)
    - `POST /api/admin/payment-methods` (إنشاء)
    - `PUT /api/admin/payment-methods/{id}` (تعديل)
    - `DELETE /api/admin/payment-methods/{id}` (حذف)

11. **صفحة إعدادات التطبيق**

    - `GET /api/admin/app-settings` (قائمة)
    - `GET /api/admin/app-settings/{id}` (تفاصيل)
    - `PUT /api/admin/app-settings/{id}` (تحديث)
    - `POST /api/admin/app-settings/update-multiple` (تحديث متعدد)
    - `DELETE /api/admin/app-settings/{id}` (حذف)

12. **صفحة مراقبة قاعدة البيانات**
    - `GET /api/admin/database/operations/latest` (أحدث العمليات)
    - `GET /api/admin/database/operations/all` (جميع العمليات)
    - `GET /api/admin/database/security-check` (فحص الأمان)

---

## 🎨 توصيات للتصميم

### 1. هيكل القائمة الجانبية (Sidebar)

**للمحاسب والمدير:**

-   Dashboard
-   المستخدمين
-   طلبات الدفع
-   الإيرادات
-   طلبات الصيانة

**للمدير فقط (إضافية):**

-   البث المباشر
-   السلايدات
-   الإشعارات
-   طرق الدفع
-   إعدادات التطبيق
-   قاعدة البيانات

### 2. Dashboard الرئيسية - الإحصائيات المطلوبة

-   عدد المستخدمين الإجمالي (`GET /api/admin/users` - count)
-   عدد المستخدمين النشطين (`GET /api/admin/users?is_active=true` - count)
-   عدد طلبات الدفع المعلقة (`GET /api/admin/payment-requests?status=0` - count)
-   إجمالي الإيرادات (`GET /api/admin/revenues/summary`)
-   عدد طلبات الصيانة المعلقة (`GET /api/admin/maintenance-requests?status=pending` - count)
-   عدد البث المباشر النشط (`GET /api/live-streams?is_active=true` - count)

### 3. معالجة الأخطاء

-   **401 Unauthorized**: إعادة توجيه لصفحة تسجيل الدخول
-   **403 Forbidden**: إخفاء العناصر غير المسموحة أو إظهار رسالة
-   **422 Validation Error**: عرض رسائل الخطأ بجانب الحقول
-   **404 Not Found**: إظهار رسالة خطأ
-   **500 Server Error**: إظهار رسالة خطأ عامة

---

## 📝 ملاحظات مهمة

1. **المصادقة**: جميع الـ routes المحمية تحتاج `Authorization: Bearer {token}` في Header
2. **Pagination**: معظم الـ endpoints التي تعرض قوائم تدعم Pagination
3. **الفلترة**: العديد من الـ endpoints تدعم معاملات فلترة متعددة
4. **رفع الملفات**: استخدم `Content-Type: multipart/form-data` عند رفع الصور
5. **الصلاحيات**: تأكد من التحقق من صلاحيات المستخدم قبل عرض الصفحات

---

## 📚 تفاصيل شاملة لكل API Endpoint

### 🌐 Routes عامة (Public)

#### 1. تسجيل الدخول

**الرابط**: `POST /api/auth/login`

**آلية الإدارة**:

-   التحقق من صحة اسم المستخدم وكلمة المرور
-   إنشاء Token باستخدام Laravel Sanctum
-   تسجيل آخر وقت تسجيل دخول

**المعاملات المطلوبة**:

```json
{
    "username": "string (required)",
    "password": "string (required)"
}
```

**الفلترة**: لا يوجد

**الرد الناجح** (200):

```json
{
    "success": true,
    "message": "تم تسجيل الدخول بنجاح",
    "data": {
        "user": {
            "id": 1,
            "username": "user001",
            "email": "user@example.com",
            "phone": "07501234567",
            "role": 0,
            "is_active": true,
            "live_access": false,
            "language": "ar"
        },
        "token": "1|xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
    }
}
```

**أخطاء محتملة**:

-   `401`: بيانات الدخول غير صحيحة
-   `422`: أخطاء التحقق من البيانات

---

#### 2. التسجيل

**الرابط**: `POST /api/auth/register`

**آلية الإدارة**:

-   التحقق من وجود المستخدم في Radius أولاً
-   التحقق من أن المستخدم غير مسجل في التطبيق
-   إنشاء حساب جديد مع Hash لكلمة المرور
-   إنشاء Token تلقائياً

**المعاملات المطلوبة**:

```json
{
    "username": "string (required, unique in radius)",
    "password": "string (required, min:6)",
    "password_confirmation": "string (required, must match password)",
    "phone": "string (required)",
    "email": "string (nullable, email format)",
    "language": "string (nullable, default: 'ar', in:['ar','en'])"
}
```

**الفلترة**: لا يوجد

**الرد الناجح** (201):

```json
{
    "success": true,
    "message": "تم إنشاء الحساب بنجاح",
    "data": {
        "user": {...},
        "token": "1|xxxxxxxxxxxxx"
    }
}
```

**أخطاء محتملة**:

-   `422`: المستخدم مسجل بالفعل / اسم المستخدم غير موجود في Radius / أخطاء التحقق

---

#### 3. عرض طرق الدفع

**الرابط**: `GET /api/payment-methods`

**آلية الإدارة**:

-   جلب جميع طرق الدفع النشطة فقط (`is_active = true`)
-   ترتيب حسب `sort_order`

**المعاملات**: لا يوجد

**الفلترة**: تلقائية (فقط النشطة)

**الرد الناجح** (200):

```json
{
    "success": true,
    "data": [
        {
            "id": 1,
            "name": "Visa",
            "name_ar": "فيزا",
            "icon": "https://example.com/icon.png",
            "qr_code": "https://example.com/qr.png",
            "code": "1234567890",
            "is_active": true,
            "instructions": "أرسل المبلغ إلى الحساب أعلاه",
            "sort_order": 1
        }
    ]
}
```

---

#### 4. عرض السلايدات

**الرابط**: `GET /api/slides`

**آلية الإدارة**:

-   جلب السلايدات المتاحة فقط (باستخدام scope `available()`)
-   التحقق من `is_active`, `start_at`, `end_at`
-   ترتيب حسب `sort_order` ثم `created_at`

**المعاملات الاختيارية**:

-   `target_audience`: string (all, active_users, expired_users)

**الفلترة**:

-   تلقائية: فقط السلايدات النشطة والمتاحة زمنياً
-   يدوية: حسب `target_audience`

**الرد الناجح** (200):

```json
{
    "success": true,
    "data": [
        {
            "id": 1,
            "title": "عرض خاص",
            "title_ar": "عرض خاص",
            "image": "https://example.com/slide.jpg",
            "link_url": "https://example.com",
            "target_audience": "all",
            "click_count": 150,
            "is_active": true
        }
    ]
}
```

---

#### 5. عرض البث المباشر

**الرابط**: `GET /api/live-streams`

**آلية الإدارة**:

-   للمستخدمين المسجلين: فلترة حسب `access_type` و `live_access` و `subscription`
-   للمستخدمين غير المسجلين: فقط `all_subscribers`
-   استخدام scope `available()` و `active()`

**المعاملات الاختيارية**:

-   `category`: string (match, channel, event)
-   `featured`: boolean (true/false)
-   `is_active`: boolean (true/false)

**الفلترة**:

-   تلقائية: حسب صلاحيات المستخدم واشتراكه
-   يدوية: حسب `category`, `featured`, `is_active`

**الرد الناجح** (200):

```json
{
    "success": true,
    "data": [
        {
            "id": 1,
            "title": "مباراة اليوم",
            "description": "وصف المباراة",
            "stream_url": "https://example.com/stream.m3u8",
            "thumbnail": "https://example.com/thumb.jpg",
            "category": "match",
            "access_type": "all_subscribers",
            "is_active": true,
            "is_featured": true,
            "view_count": 150,
            "start_time": "2024-12-25T20:00:00Z",
            "end_time": "2024-12-25T22:00:00Z"
        }
    ],
    "meta": {
        "current_page": 1,
        "last_page": 1,
        "per_page": 15,
        "total": 1
    }
}
```

---

### 🔒 Routes محمية

#### 8. الحصول على المستخدم الحالي

**الرابط**: `GET /api/auth/me`

**آلية الإدارة**:

-   جلب بيانات المستخدم من Token
-   إرجاع معلومات كاملة عن المستخدم

**المعاملات**: لا يوجد

**الفلترة**: لا يوجد

**الرد الناجح** (200):

```json
{
    "success": true,
    "data": {
        "id": 1,
        "username": "user001",
        "email": "user@example.com",
        "phone": "07501234567",
        "role": 0,
        "is_active": true,
        "live_access": false,
        "language": "ar",
        "last_login_at": "2024-12-25T10:00:00Z",
        "created_at": "2024-01-01T00:00:00Z"
    }
}
```

---

#### 10. عرض الملف الشخصي

**الرابط**: `GET /api/user/profile`

**آلية الإدارة**:

-   جلب بيانات المستخدم مع الاشتراك المحفوظ
-   عرض معلومات الاشتراك من `user_subscriptions` table

**المعاملات**: لا يوجد

**الفلترة**: لا يوجد

**الرد الناجح** (200):

```json
{
    "success": true,
    "data": {
        "user": {
            "id": 1,
            "username": "user001",
            "email": "user@example.com",
            "phone": "07501234567",
            "role": 0,
            "is_active": true,
            "live_access": false,
            "language": "ar"
        },
        "subscription": {
            "id": 1,
            "radius_username": "user001",
            "expires_at": "2026-01-02T10:00:00Z",
            "balance": 8.0,
            "plan_name": "2M-PPP",
            "last_synced_at": "2025-12-10T09:00:00Z",
            "is_active": true
        }
    }
}
```

---

#### 11. تحديث الملف الشخصي

**الرابط**: `PUT /api/user/profile`

**آلية الإدارة**:

-   تحديث الحقول المرسلة فقط
-   حفظ `device_token` و `device_type` في `device_tokens` table

**المعاملات الاختيارية**:

```json
{
    "phone": "string (nullable)",
    "email": "string (nullable, email)",
    "language": "string (nullable, in:['ar','en'])",
    "device_token": "string (nullable)",
    "device_type": "string (nullable, in:['android','ios'])"
}
```

**الفلترة**: لا يوجد

**الرد الناجح** (200):

```json
{
    "success": true,
    "message": "تم تحديث الملف الشخصي بنجاح",
    "data": {
        "user": {...}
    }
}
```

---

#### 16. عرض طلبات الدفع

**الرابط**: `GET /api/payment-requests`

**آلية الإدارة**:

-   **المستخدم العادي**: فقط طلباته الخاصة (`user_id = current_user.id`)
-   **المحاسب/المدير**: جميع الطلبات
-   Pagination تلقائي (15 عنصر لكل صفحة)

**المعاملات الاختيارية**:

-   `status`: integer (0: pending, 1: approved, 2: rejected)
-   `per_page`: integer (افتراضي: 15)
-   `page`: integer (افتراضي: 1)

**الفلترة**:

-   تلقائية: حسب صلاحيات المستخدم
-   يدوية: حسب `status`

**الرد الناجح** (200):

```json
{
    "success": true,
    "data": [
        {
            "id": 1,
            "user": {
                "id": 1,
                "username": "user001"
            },
            "amount": 50000,
            "currency": "IQD",
            "status": 0,
            "status_label": "قيد المراجعة",
            "payment_method_details": {...},
            "receipt_file": "https://example.com/receipt.jpg",
            "created_at": "2024-12-25T10:00:00Z"
        }
    ],
    "meta": {
        "current_page": 1,
        "last_page": 5,
        "per_page": 15,
        "total": 75
    }
}
```

---

#### 17. إنشاء طلب دفع جديد

**الرابط**: `POST /api/payment-requests`  
**Content-Type**: `multipart/form-data`

**آلية الإدارة**:

-   حفظ الملف المرفوع في `storage/app/public/receipts/`
-   إنشاء سجل في `payment_requests` table
-   الحالة الافتراضية: `status = 0` (pending)

**المعاملات المطلوبة**:

```json
{
    "amount": "number (required, min:0.01)",
    "currency": "string (nullable, in:['IQD','USD'], default: 'IQD')",
    "period_months": "integer (nullable, min:1, max:12)",
    "payment_method_id": "integer (nullable, exists:payment_methods,id)",
    "payment_method": "string (nullable, max:50)",
    "transaction_number": "string (nullable, max:100)",
    "receipt_file": "file (nullable, mimes:jpg,jpeg,png,pdf, max:5120KB)",
    "payment_date": "date (nullable, format: Y-m-d)"
}
```

**الفلترة**: لا يوجد

**الرد الناجح** (201):

```json
{
    "success": true,
    "message": "تم إرسال طلب الدفع بنجاح",
    "data": {
        "id": 1,
        "amount": 50000,
        "currency": "IQD",
        "status": 0,
        "status_label": "قيد المراجعة",
        "receipt_file": "https://example.com/storage/receipts/receipt_123.jpg",
        "created_at": "2024-12-25T10:00:00Z"
    }
}
```

**أخطاء محتملة**:

-   `422`: أخطاء التحقق (مثل: الملف كبير جداً، نوع الملف غير مدعوم)

---

#### 28. عرض جميع طلبات الدفع (Admin/Accountant)

**الرابط**: `GET /api/admin/payment-requests`

**آلية الإدارة**:

-   جلب جميع الطلبات بدون قيود
-   استخدام `PaymentService` للفلترة المتقدمة
-   Pagination تلقائي

**المعاملات الاختيارية**:

-   `status`: integer (0: pending, 1: approved, 2: rejected)
-   `is_paid`: boolean (true/false/null)
-   `is_deferred`: boolean (true/false/null)
-   `user_id`: integer
-   `payment_method_id`: integer
-   `per_page`: integer (افتراضي: 15)
-   `page`: integer (افتراضي: 1)

**الفلترة**:

-   حسب `status`: قيد المراجعة / مقبول / مرفوض
-   حسب `is_paid`: مدفوع / غير مدفوع
-   حسب `is_deferred`: مؤجل / غير مؤجل
-   حسب `user_id`: مستخدم محدد
-   حسب `payment_method_id`: طريقة دفع محددة

**الرد الناجح** (200):

```json
{
    "success": true,
    "data": [
        {
            "id": 1,
            "user": {
                "id": 1,
                "username": "user001",
                "email": "user@example.com"
            },
            "amount": 50000,
            "approved_amount": null,
            "currency": "IQD",
            "status": 0,
            "status_label": "قيد المراجعة",
            "is_paid": false,
            "is_deferred": false,
            "payment_status_label": "غير مدفوع",
            "payment_method_details": {...},
            "receipt_file": "https://example.com/receipt.jpg",
            "notes": null,
            "created_at": "2024-12-25T10:00:00Z"
        }
    ],
    "meta": {
        "current_page": 1,
        "last_page": 5,
        "per_page": 15,
        "total": 75
    }
}
```

---

#### 29. قبول/رفض طلب دفع

**الرابط**: `PUT /api/admin/payment-requests/{id}/status`

**آلية الإدارة**:

-   تحديث حالة الطلب
-   إذا كان `status = 1` (approved):
    -   إنشاء سجل في `revenues` table
    -   تحديث `user_subscriptions` (تجديد الاشتراك)
    -   إرسال إشعار للمستخدم
-   إذا كان `status = 2` (rejected):
    -   حفظ `reject_reason`
    -   إرسال إشعار للمستخدم
-   حفظ `reviewer_id` و `reviewed_at`

**المعاملات المطلوبة**:

```json
{
    "status": "integer (required, in:[1,2])",
    "reject_reason": "string (required_if:status,2, max:500)",
    "notes": "string (nullable, max:1000)",
    "approved_amount": "number (nullable, min:0.01)",
    "period_months": "integer (required_if:status,1, min:1, max:12)",
    "plan_name": "string (nullable, max:100)"
}
```

**الفلترة**: لا يوجد

**الرد الناجح** (200):

```json
{
    "success": true,
    "message": "تم قبول الطلب بنجاح",
    "data": {
        "id": 1,
        "status": 1,
        "status_label": "مقبول",
        "approved_amount": 50000,
        "period_months": 1,
        "plan_name": "2M-PPP",
        "reviewer": {
            "id": 2,
            "username": "accountant001"
        },
        "reviewed_at": "2024-12-25T11:00:00Z",
        "notes": "تم قبول الطلب",
        "revenue": {
            "id": 1,
            "amount": 50000,
            "currency": "IQD",
            "payment_date": "2024-12-25"
        }
    }
}
```

**أخطاء محتملة**:

-   `403`: ليس لديك صلاحية
-   `422`: أخطاء التحقق (مثل: `period_months` مطلوب عند القبول)

---

#### 30. إنشاء دفع نقدي مباشر

**الرابط**: `POST /api/admin/payment-requests/cash-payment`

**آلية الإدارة**:

-   إنشاء طلب دفع نقدي مباشر بدون انتظار موافقة
-   الحالة تلقائياً: `status = 1` (approved)
-   `payment_type = 'cash'`
-   إنشاء سجل في `revenues` تلقائياً
-   تحديث الاشتراك تلقائياً
-   إذا كان `is_deferred = true`: لا يتم إنشاء revenue حتى يتم الدفع

**المعاملات المطلوبة**:

```json
{
    "user_id": "integer (required, exists:app_users,id)",
    "amount": "number (required, min:0.01)",
    "currency": "string (nullable, default: 'IQD')",
    "period_months": "integer (required, min:1, max:12)",
    "payment_method_id": "integer (nullable)",
    "payment_date": "date (nullable, default: today)",
    "plan_name": "string (nullable)",
    "notes": "string (nullable)",
    "is_deferred": "boolean (nullable, default: false)"
}
```

**الفلترة**: لا يوجد

**الرد الناجح** (201):

```json
{
    "success": true,
    "message": "تم إضافة الدفعة النقدية بنجاح",
    "data": {
        "id": 1,
        "payment_type": "cash",
        "payment_type_label": "نقدي",
        "amount": 50000,
        "status": 1,
        "is_deferred": false,
        "created_by": {
            "id": 2,
            "username": "accountant001"
        },
        "user": {...},
        "revenue": {...}
    }
}
```

---

#### 32. عرض جميع المستخدمين

**الرابط**: `GET /api/admin/users`

**آلية الإدارة**:

-   جلب جميع المستخدمين مع العلاقات (`subscription`)
-   البحث في `username`, `phone`, `email`
-   فلترة متعددة
-   ترتيب قابل للتخصيص
-   Pagination تلقائي

**المعاملات الاختيارية**:

-   `search`: string (البحث في username, phone, email)
-   `role`: integer (0: user, 1: accountant, 2: admin)
-   `is_active`: boolean (true/false)
-   `live_access`: boolean (true/false)
-   `language`: string (ar/en)
-   `sort_by`: string (created_at, username, email, etc.)
-   `sort_order`: string (asc/desc, default: desc)
-   `per_page`: integer (افتراضي: 15)
-   `page`: integer (افتراضي: 1)

**الفلترة**:

-   **البحث**: `search` يبحث في username, phone, email
-   **حسب الدور**: `role` (0, 1, 2)
-   **حسب الحالة**: `is_active` (true/false)
-   **حسب صلاحية البث**: `live_access` (true/false)
-   **حسب اللغة**: `language` (ar/en)

**الترتيب**:

-   حسب `sort_by` (افتراضي: created_at)
-   اتجاه `sort_order` (افتراضي: desc)

**الرد الناجح** (200):

```json
{
    "success": true,
    "data": [
        {
            "id": 1,
            "username": "user001",
            "phone": "07501234567",
            "email": "user@example.com",
            "role": 0,
            "is_active": true,
            "live_access": true,
            "language": "ar",
            "last_login_at": "2024-12-25T10:00:00Z",
            "created_at": "2024-01-01T00:00:00Z",
            "updated_at": "2024-12-25T10:00:00Z"
        }
    ],
    "meta": {
        "current_page": 1,
        "last_page": 5,
        "per_page": 15,
        "total": 75
    }
}
```

---

#### 33. عرض تفاصيل مستخدم محدد

**الرابط**: `GET /api/admin/users/{id}`

**آلية الإدارة**:

-   جلب المستخدم مع جميع العلاقات:
    -   `subscription`: معلومات الاشتراك
    -   `paymentRequests`: طلبات الدفع
    -   `deviceTokens`: أجهزة المستخدم

**المعاملات**: لا يوجد (ID في URL)

**الفلترة**: لا يوجد

**الرد الناجح** (200):

```json
{
    "success": true,
    "data": {
        "id": 1,
        "username": "user001",
        "phone": "07501234567",
        "email": "user@example.com",
        "role": 0,
        "is_active": true,
        "live_access": true,
        "language": "ar",
        "subscription": {
            "id": 1,
            "radius_username": "user001",
            "expires_at": "2026-01-02T10:00:00Z",
            "is_active": true
        },
        "payment_requests": [
            {
                "id": 1,
                "amount": 50000,
                "status": 1,
                "created_at": "2024-01-01T00:00:00Z"
            }
        ],
        "device_tokens": [...]
    }
}
```

---

#### 34. تحديث مستخدم

**الرابط**: `PUT /api/admin/users/{id}`

**آلية الإدارة**:

-   تحديث الحقول المرسلة فقط (جميعها اختيارية)
-   إذا تم تغيير `username`: تحديث `radius_username` في الاشتراك تلقائياً
-   إذا تم إرسال `password`: Hash تلقائياً
-   استخدام Database Transaction

**المعاملات الاختيارية** (جميعها):

```json
{
    "username": "string (nullable, unique)",
    "password": "string (nullable, min:6)",
    "phone": "string (nullable)",
    "email": "string (nullable, email)",
    "language": "string (nullable, in:['ar','en'])",
    "is_active": "boolean (nullable)",
    "live_access": "boolean (nullable)",
    "role": "integer (nullable, in:[0,1,2])"
}
```

**الفلترة**: لا يوجد

**الرد الناجح** (200):

```json
{
    "success": true,
    "message": "تم تحديث المستخدم بنجاح",
    "data": {
        "id": 1,
        "username": "new_username",
        "email": "new@example.com",
        ...
    }
}
```

---

#### 40. عرض جميع الإيرادات

**الرابط**: `GET /api/admin/revenues`

**آلية الإدارة**:

-   جلب جميع الإيرادات من `revenues` table
-   العلاقات: `user`, `paymentRequest`
-   حساب ملخص تلقائي (إجمالي، عدد المعاملات، المتوسط)

**المعاملات الاختيارية**:

-   `user_id`: integer
-   `from_date`: date (Y-m-d)
-   `to_date`: date (Y-m-d)
-   `payment_type`: string
-   `currency`: string (IQD/USD)
-   `sort_by`: string (افتراضي: payment_date)
-   `sort_order`: string (افتراضي: desc)
-   `per_page`: integer (افتراضي: 15)
-   `page`: integer (افتراضي: 1)

**الفلترة**:

-   حسب المستخدم: `user_id`
-   حسب التاريخ: `from_date`, `to_date`
-   حسب نوع الدفع: `payment_type`
-   حسب العملة: `currency`

**الرد الناجح** (200):

```json
{
    "success": true,
    "data": [
        {
            "id": 1,
            "user": {
                "id": 1,
                "username": "user001"
            },
            "amount": 50000,
            "currency": "IQD",
            "payment_type": "online",
            "payment_date": "2024-12-25",
            "period_months": 1,
            "payment_request": {
                "id": 1,
                "status": 1
            }
        }
    ],
    "summary": {
        "total_revenue": 1500000.0,
        "total_transactions": 30,
        "average_revenue": 50000.0
    },
    "meta": {
        "current_page": 1,
        "last_page": 2,
        "per_page": 15,
        "total": 30
    }
}
```

---

#### 41. ملخص الإيرادات

**الرابط**: `GET /api/admin/revenues/summary`

**آلية الإدارة**:

-   حساب إجمالي الإيرادات
-   تجميع حسب نوع الدفع (`payment_type`)
-   تجميع حسب العملة (`currency`)
-   إيرادات يومية (آخر 30 يوم)
-   أفضل 10 مستخدمين حسب الإيرادات

**المعاملات الاختيارية**:

-   `from_date`: date (Y-m-d)
-   `to_date`: date (Y-m-d)
-   `user_id`: integer

**الفلترة**:

-   حسب التاريخ: `from_date`, `to_date`
-   حسب المستخدم: `user_id`

**الرد الناجح** (200):

```json
{
    "success": true,
    "data": {
        "total_revenue": 1500000.0,
        "total_transactions": 30,
        "average_revenue": 50000.0,
        "by_payment_type": [
            {
                "payment_type": "online",
                "total": 1000000.0,
                "count": 20
            },
            {
                "payment_type": "cash",
                "total": 500000.0,
                "count": 10
            }
        ],
        "by_currency": [
            {
                "currency": "IQD",
                "total": 1500000.0,
                "count": 30
            }
        ],
        "daily_revenue": [
            {
                "date": "2024-12-25",
                "total": 50000.0,
                "count": 1
            }
        ],
        "top_users": [
            {
                "id": 1,
                "username": "user001",
                "phone": "07501234567",
                "total": 500000.0,
                "count": 10
            }
        ]
    }
}
```

---

#### 42. المدفوعات المؤجلة

**الرابط**: `GET /api/admin/revenues/deferred-payments`

**آلية الإدارة**:

-   جلب طلبات الدفع التي `is_deferred = true` و `is_paid = false`
-   العلاقات: `user`, `paymentMethod`
-   حساب إجمالي المبلغ المؤجل

**المعاملات الاختيارية**:

-   `user_id`: integer
-   `from_date`: date (Y-m-d)
-   `to_date`: date (Y-m-d)
-   `per_page`: integer (افتراضي: 15)
-   `page`: integer (افتراضي: 1)

**الفلترة**:

-   تلقائية: فقط المؤجلة وغير المدفوعة
-   يدوية: حسب `user_id`, `from_date`, `to_date`

**الرد الناجح** (200):

```json
{
    "success": true,
    "data": [
        {
            "id": 1,
            "user": {
                "id": 1,
                "username": "user001"
            },
            "amount": 50000,
            "is_deferred": true,
            "is_paid": false,
            "created_at": "2024-12-25T10:00:00Z"
        }
    ],
    "summary": {
        "total_deferred_amount": 500000.0,
        "total_deferred_count": 10
    },
    "meta": {
        "current_page": 1,
        "last_page": 1,
        "per_page": 15,
        "total": 10
    }
}
```

---

#### 38. عرض جميع طلبات الصيانة

**الرابط**: `GET /api/admin/maintenance-requests`

**آلية الإدارة**:

-   جلب جميع طلبات الصيانة
-   العلاقات: `user`, `assignedTo`
-   ترتيب حسب `created_at` (الأحدث أولاً)

**المعاملات الاختيارية**:

-   `status`: string (pending, in_progress, completed, cancelled)
-   `user_id`: integer
-   `per_page`: integer (افتراضي: 15)
-   `page`: integer (افتراضي: 1)

**الفلترة**:

-   حسب الحالة: `status`
-   حسب المستخدم: `user_id`

**الرد الناجح** (200):

```json
{
    "success": true,
    "data": [
        {
            "id": 1,
            "user": {
                "id": 1,
                "username": "user001"
            },
            "address": "عنوان الصيانة",
            "description": "وصف المشكلة",
            "status": "pending",
            "assigned_to": null,
            "subscription_data": {...},
            "created_at": "2024-12-25T10:00:00Z"
        }
    ],
    "meta": {
        "current_page": 1,
        "last_page": 1,
        "per_page": 15,
        "total": 1
    }
}
```

---

#### 39. تحديث حالة طلب الصيانة

**الرابط**: `PUT /api/admin/maintenance-requests/{id}/status`

**آلية الإدارة**:

-   تحديث حالة الطلب
-   إذا كانت الحالة `completed`: حفظ `completed_at` تلقائياً
-   تحديث `assigned_to` إذا تم إرساله

**المعاملات المطلوبة**:

```json
{
    "status": "string (required, in:['pending','in_progress','completed','cancelled'])",
    "assigned_to": "integer (nullable, exists:app_users,id)",
    "notes": "string (nullable)"
}
```

**الفلترة**: لا يوجد

**الرد الناجح** (200):

```json
{
    "success": true,
    "message": "تم تحديث حالة الطلب بنجاح",
    "data": {
        "id": 1,
        "status": "completed",
        "assigned_to": {
            "id": 2,
            "username": "technician001"
        },
        "completed_at": "2024-12-25T12:00:00Z",
        "notes": "تم إصلاح المشكلة"
    }
}
```

---

#### 43. إنشاء بث مباشر جديد

**الرابط**: `POST /api/admin/live-streams`

**آلية الإدارة**:

-   إنشاء سجل جديد في `live_streams` table
-   إذا كان `start_time` اليوم: إرسال إشعار تلقائي للمستخدمين المناسبين
-   تحديد المستخدمين حسب `access_type`:
    -   `all_subscribers`: جميع المشتركين النشطين
    -   `live_subscribers_only`: المشتركين مع `live_access = true`

**المعاملات المطلوبة**:

```json
{
    "title": "string (required, max:150)",
    "description": "string (nullable)",
    "stream_url": "string (required, url, max:500)",
    "access_type": "string (nullable, in:['all_subscribers','live_subscribers_only'], default: 'all_subscribers')",
    "thumbnail": "string (nullable, url, max:255)",
    "category": "string (nullable, in:['match','channel','event'])",
    "stream_type": "string (nullable, in:['live','vod'], default: 'live')",
    "is_active": "boolean (nullable, default: true)",
    "is_featured": "boolean (nullable, default: false)",
    "start_time": "date (nullable, format: Y-m-d H:i:s)",
    "end_time": "date (nullable, format: Y-m-d H:i:s, after:start_time)",
    "max_viewers": "integer (nullable, min:1)",
    "sort_order": "integer (nullable)",
    "quality_options": "array (nullable)"
}
```

**الفلترة**: لا يوجد

**الرد الناجح** (201):

```json
{
    "success": true,
    "message": "تم إنشاء البث بنجاح",
    "data": {
        "id": 1,
        "title": "مباراة اليوم",
        "stream_url": "https://example.com/stream.m3u8",
        "access_type": "all_subscribers",
        "is_active": true,
        "is_featured": true,
        "view_count": 0,
        "created_at": "2024-12-25T10:00:00Z"
    }
}
```

---

#### 46. إنشاء سلايد جديد

**الرابط**: `POST /api/admin/slides`  
**Content-Type**: `multipart/form-data`

**آلية الإدارة**:

-   رفع الصورة وحفظها في `storage/app/public/slides/`
-   إنشاء سجل في `slides` table
-   `click_count` يبدأ من 0

**المعاملات المطلوبة**:

```json
{
    "title": "string (required)",
    "title_ar": "string (nullable)",
    "description": "string (nullable)",
    "description_ar": "string (nullable)",
    "image": "file (required, image, max:5120KB)",
    "link_url": "string (nullable, url)",
    "target_audience": "string (nullable, in:['all','active_users','expired_users'], default: 'all')",
    "is_active": "boolean (nullable, default: true)",
    "sort_order": "integer (nullable, default: 0)",
    "start_at": "date (nullable, format: Y-m-d H:i:s)",
    "end_at": "date (nullable, format: Y-m-d H:i:s, after:start_at)"
}
```

**الفلترة**: لا يوجد

**الرد الناجح** (201):

```json
{
    "success": true,
    "message": "تم إنشاء السلايد بنجاح",
    "data": {
        "id": 1,
        "title": "عرض خاص",
        "image": "https://example.com/storage/slides/slide_123.jpg",
        "link_url": "https://example.com",
        "target_audience": "all",
        "click_count": 0,
        "is_active": true,
        "sort_order": 1
    }
}
```

---

#### 49. إنشاء وإرسال إشعار

**الرابط**: `POST /api/admin/notifications`

**آلية الإدارة**:

-   إنشاء إشعار في `notifications` table
-   تحديد المستلمين حسب `target_type`:
    -   `all`: جميع المستخدمين النشطين
    -   `active`: المستخدمين النشطين مع اشتراك صالح
    -   `specific`: المستخدمين المحددين في `user_ids`
-   إرسال Push Notification عبر Firebase
-   حفظ العلاقة في `notification_user` table

**المعاملات المطلوبة**:

```json
{
    "title": "string (required, max:150)",
    "body": "string (required)",
    "type": "string (nullable, in:['system','manual'], default: 'manual')",
    "priority": "integer (nullable, in:[0,1,2], default: 0)",
    "action_url": "string (nullable, url, max:255)",
    "action_text": "string (nullable, max:50)",
    "icon": "string (nullable, max:255)",
    "sound": "string (nullable, max:255)",
    "badge": "integer (nullable)",
    "target_type": "string (nullable, in:['all','active','specific'], default: 'all')",
    "user_ids": "array (required_if:target_type,specific, array of integers)"
}
```

**الفلترة**: لا يوجد

**الرد الناجح** (201):

```json
{
    "success": true,
    "message": "تم إرسال الإشعار بنجاح",
    "data": {
        "id": 1,
        "title": "عنوان الإشعار",
        "body": "محتوى الإشعار",
        "type": "manual",
        "priority": 1,
        "created_by": {
            "id": 2,
            "username": "admin001"
        },
        "created_at": "2024-12-25T10:00:00Z"
    }
}
```

---

#### 51. إنشاء طريقة دفع جديدة

**الرابط**: `POST /api/admin/payment-methods`  
**Content-Type**: `multipart/form-data`

**آلية الإدارة**:

-   رفع `icon` و `qr_code` (اختياري) وحفظهما في `storage/app/public/payment-methods/`
-   إنشاء سجل في `payment_methods` table

**المعاملات المطلوبة**:

```json
{
    "name": "string (required)",
    "name_ar": "string (nullable)",
    "icon": "file (required, image, max:5120KB)",
    "qr_code": "file (nullable, image, max:5120KB)",
    "code": "string (nullable, max:100)",
    "is_active": "boolean (nullable, default: true)",
    "instructions": "string (nullable)",
    "sort_order": "integer (nullable, default: 0)"
}
```

**الفلترة**: لا يوجد

**الرد الناجح** (201):

```json
{
    "success": true,
    "message": "تم إنشاء طريقة الدفع بنجاح",
    "data": {
        "id": 1,
        "name": "Visa",
        "name_ar": "فيزا",
        "icon": "https://example.com/storage/payment-methods/icon_123.png",
        "qr_code": "https://example.com/storage/payment-methods/qr_123.png",
        "code": "1234567890",
        "is_active": true,
        "instructions": "أرسل المبلغ إلى الحساب أعلاه",
        "sort_order": 1
    }
}
```

---

#### 54. عرض جميع الإعدادات (Admin)

**الرابط**: `GET /api/admin/app-settings`

**آلية الإدارة**:

-   جلب جميع الإعدادات (بما فيها غير النشطة)
-   ترتيب حسب `sort_order` ثم `key`

**المعاملات الاختيارية**:

-   `type`: string (social_link, general_setting)
-   `is_active`: boolean (true/false/null)

**الفلترة**:

-   حسب النوع: `type`
-   حسب الحالة: `is_active`

**الرد الناجح** (200):

```json
{
    "success": true,
    "data": [
        {
            "id": 1,
            "key": "facebook_url",
            "value": "https://facebook.com/page",
            "value_ar": "https://facebook.com/page",
            "type": "social_link",
            "label": "رابط فيسبوك",
            "label_en": "Facebook URL",
            "description": "رابط صفحة فيسبوك",
            "is_active": true,
            "sort_order": 1
        }
    ]
}
```

---

#### 57. تحديث عدة إعدادات دفعة واحدة

**الرابط**: `POST /api/admin/app-settings/update-multiple`

**آلية الإدارة**:

-   استخدام `updateOrCreate` لكل إعداد
-   إذا كان `key` موجود: تحديث
-   إذا كان `key` غير موجود: إنشاء جديد

**المعاملات المطلوبة**:

```json
{
    "settings": [
        {
            "key": "string (required, max:100)",
            "value": "string (nullable, max:1000)",
            "value_ar": "string (nullable, max:1000)",
            "type": "string (nullable, in:['social_link','general_setting'])",
            "label": "string (nullable, max:255)",
            "label_en": "string (nullable, max:255)",
            "description": "string (nullable, max:500)",
            "is_active": "boolean (nullable, default: true)",
            "sort_order": "integer (nullable, default: 0)"
        }
    ]
}
```

**الفلترة**: لا يوجد

**الرد الناجح** (200):

```json
{
    "success": true,
    "message": "تم تحديث الإعدادات بنجاح",
    "data": [
        {
            "id": 1,
            "key": "facebook_url",
            "value": "https://facebook.com/new-page",
            ...
        }
    ]
}
```

---

#### 59. أحدث عمليات قاعدة البيانات

**الرابط**: `GET /api/admin/database/operations/latest`

**آلية الإدارة**:

-   جلب أحدث العمليات من عدة جداول:
    -   `payment_requests`
    -   `revenues`
    -   `app_users`
    -   وغيرها
-   دمج النتائج وترتيبها حسب `created_at`

**المعاملات الاختيارية**:

-   `limit`: integer (افتراضي: 50)

**الفلترة**: تلقائية (الأحدث أولاً)

**الرد الناجح** (200):

```json
{
    "success": true,
    "data": [
        {
            "id": 1,
            "table": "payment_requests",
            "type": "insert",
            "data": {
                "user_id": 1,
                "amount": 50000,
                "status": 0
            },
            "timestamp": "2024-12-25T10:00:00Z"
        }
    ]
}
```

---

## 🔍 ملخص آلية الفلترة والبحث

### البحث (Search)

-   **المستخدمين**: البحث في `username`, `phone`, `email` (حساس لحالة الأحرف)
-   **البحث النصي**: استخدام `LIKE` مع `%search%`

### الفلترة (Filtering)

-   **حسب الحالة**: `is_active`, `status`, `is_paid`, `is_deferred`
-   **حسب التاريخ**: `from_date`, `to_date`, `start_date`, `end_date`
-   **حسب المستخدم**: `user_id`
-   **حسب الدور**: `role` (0, 1, 2)
-   **حسب النوع**: `category`, `type`, `payment_type`, `currency`

### الترتيب (Sorting)

-   **افتراضي**: `created_at DESC` (الأحدث أولاً)
-   **قابل للتخصيص**: `sort_by` + `sort_order` (asc/desc)

### الترقيم (Pagination)

-   **افتراضي**: 15 عنصر لكل صفحة
-   **قابل للتخصيص**: `per_page`
-   **الاستجابة**: `meta` object مع `current_page`, `last_page`, `total`

---

**تم إنشاء هذا الدليل بتاريخ:** 2025-12-25  
**للاستخدام في:** تصميم وتطوير لوحة التحكم (Dashboard)
