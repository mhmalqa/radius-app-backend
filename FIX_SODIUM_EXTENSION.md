# 🔧 حل مشكلة ext-sodium في PHP

## المشكلة

حزمة `kreait/firebase-php` تتطلب `ext-sodium` وهو غير مفعّل في PHP.

---

## الحل 1: تفعيل ext-sodium (الطريقة الموصى بها)

### الخطوة 1: فتح ملف php.ini

افتح ملف `php.ini` من المسار:
```
C:\xampp\php\php.ini
```

### الخطوة 2: البحث عن sodium

ابحث عن:
```ini
;extension=sodium
```

أو
```ini
;extension=libsodium
```

### الخطوة 3: إزالة التعليق

غيّر من:
```ini
;extension=sodium
```

إلى:
```ini
extension=sodium
```

### الخطوة 4: حفظ الملف وإعادة تشغيل Apache

1. احفظ الملف
2. أعد تشغيل Apache من XAMPP Control Panel

### الخطوة 5: التحقق من التفعيل

```bash
php -m | findstr sodium
```

إذا ظهر `sodium`، يعني أنه مفعّل بنجاح.

### الخطوة 6: إعادة تثبيت الحزمة

```bash
composer require kreait/firebase-php
```

---

## الحل 2: استخدام حل بديل (إذا لم يعمل الحل 1)

إذا لم تستطع تفعيل `ext-sodium`، يمكنك استخدام:

### أ. تثبيت مع تجاهل المتطلبات:

```bash
composer require kreait/firebase-php --ignore-platform-req=ext-sodium
```

**ملاحظة:** هذا حل مؤقت وقد يسبب مشاكل لاحقاً.

### ب. استخدام إصدار أقدم:

```bash
composer require kreait/firebase-php:^6.0
```

---

## الحل 3: استخدام HTTP API مباشرة (بدون حزمة)

إذا لم تعمل الحلول السابقة، يمكننا تعديل `FirebaseMessagingService` لاستخدام HTTP API مباشرة (وهو يعمل بالفعل كـ fallback).

---

## التحقق من إصدار PHP

```bash
php -v
```

تأكد من أنك تستخدم PHP 7.4 أو أحدث.

---

## إذا كان sodium غير موجود في php.ini

إذا لم تجد `extension=sodium` في `php.ini`، أضف هذه السطر في نهاية الملف:

```ini
extension=sodium
```

ثم أعد تشغيل Apache.

